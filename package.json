{
  "name": "el-react-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://el-gosia.github.io/el-react-app",
  "dependencies": {
    "classnames": "^2.2.6",
    "gh-pages": "^2.0.1",
    "node-sass": "^4.11.0",
    "normalize.css": "^8.0.1",
    "prop-types": "^15.6.2",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-loader": "^2.4.5",
    "react-scripts": "2.1.3"
  },
  "scripts": {
    "start": "react-scripts start | json-server --watch issues-db.json --port 9000",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "lint": "eslint src/**/*.js",
    "stylelint": "stylelint src/**/*.{css,scss}",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "prettier"
    ]
  },
  "browserslist": [
    "last 1 version",
    "not dead",
    "not ie > 0",
    "not op_mini all"
  ],
  "devDependencies": {
    "eslint-config-prettier": "^4.0.0",
    "json-server": "^0.14.2",
    "stylelint": "^9.10.1",
    "stylelint-config-prettier": "^4.0.0",
    "stylelint-config-recommended": "^2.1.0",
    "stylelint-scss": "^3.5.2"
  }
}
